<!--
Checklist before submitting:

* Add a test case to the YAML files under cmd/*def/ for any behavior change.
* Grammar changes go in parser/parser.y only. parser/parser.go is generated and
  has a single writer: it is regenerated on master after every merge, and CI
  rejects a pull request whose diff touches it. `make parser` leaving it dirty
  in your working tree is expected - just leave it out of your commits.
-->
