name: sqldef
on:
  push:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo ${{ github.event }}
      - run: echo ${{ github.event_name }}
