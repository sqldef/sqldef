CreateTable: |
  CREATE TABLE bigdata (
    data bigint
  );
CreateTableSchema: |
  CREATE TABLE public.bigdata (
    data bigint
  );
CreateView: |
  create view hoge_view as
  select amount from hoge;
