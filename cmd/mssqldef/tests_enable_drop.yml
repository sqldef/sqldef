# yaml-language-server: $schema=../../testutil/testcase.schema.json
# Tests for enable_drop: false behavior (DROP statements are commented out)
# The enable_drop: true behavior is tested in other test files.

DropTableSkipped:
  enable_drop: false
  current: |
    CREATE TABLE users (id bigint NOT NULL);
  desired: ""
  up: |
    -- Skipped: DROP TABLE [dbo].[users];
  down: ""

DropViewSkipped:
  enable_drop: false
  current: |
    CREATE TABLE users (id bigint NOT NULL, name varchar(100));
    CREATE VIEW users_view AS SELECT id, name FROM users;
  desired: |
    CREATE TABLE users (id bigint NOT NULL, name varchar(100));
  up: |
    -- Skipped: DROP VIEW [dbo].[users_view];
  down: ""

DropColumnSkipped:
  enable_drop: false
  current: |
    CREATE TABLE users (
      id bigint NOT NULL,
      name varchar(100)
    );
  desired: |
    CREATE TABLE users (
      id bigint NOT NULL
    );
  up: |
    -- Skipped: ALTER TABLE [dbo].[users] DROP COLUMN [name];
  down: ""

DropIndexSkipped:
  enable_drop: false
  current: |
    CREATE TABLE users (
      id bigint NOT NULL,
      email varchar(255)
    );
    CREATE INDEX idx_email ON users (email);
  desired: |
    CREATE TABLE users (
      id bigint NOT NULL,
      email varchar(255)
    );
  up: |
    -- Skipped: DROP INDEX [idx_email] ON [dbo].[users];
  down: ""

AddCheckSpellingDropColumnNotSkipped:
  enable_drop: false
  legacy_ignore_quotes: false
  current: |
    CREATE TABLE users (id bigint NOT NULL, note varchar(40));
  desired: |
    CREATE TABLE users (
      id bigint NOT NULL,
      note varchar(40),
      CONSTRAINT note_ck CHECK (note != 'set before DROP COLUMN runs')
    );
  up: |
    ALTER TABLE dbo.users ADD CONSTRAINT note_ck CHECK (note != 'set before DROP COLUMN runs');
  down: |
    ALTER TABLE dbo.users DROP CONSTRAINT [note_ck];
