# yaml-language-server: $schema=../../testutil/testcase.schema.json

CreateTableAutoRandom:
  flavor: tidb
  legacy_ignore_quotes: false
  desired: |
    CREATE TABLE t (
      id bigint AUTO_RANDOM,
      PRIMARY KEY (id)
    );

CreateTableAutoRandomWithShardBits:
  flavor: tidb
  legacy_ignore_quotes: false
  desired: |
    CREATE TABLE t (
      id bigint AUTO_RANDOM(5),
      PRIMARY KEY (id)
    );

CreateTableAutoRandomWithShardBitsAndRange:
  flavor: tidb
  legacy_ignore_quotes: false
  desired: |
    CREATE TABLE t (
      id bigint AUTO_RANDOM(5, 54),
      PRIMARY KEY (id)
    );

CreateTableAutoRandomTiDBComment:
  flavor: tidb
  legacy_ignore_quotes: false
  current: |
    CREATE TABLE t (
      id bigint /*T![auto_rand] AUTO_RANDOM(5) */ NOT NULL,
      PRIMARY KEY (id)
    );
  desired: |
    CREATE TABLE t (
      id bigint AUTO_RANDOM(5) NOT NULL,
      PRIMARY KEY (id)
    );

AddAutoRandom:
  legacy_ignore_quotes: false
  offline: true
  current: |
    CREATE TABLE t (
      id bigint NOT NULL,
      PRIMARY KEY (id)
    );
  desired: |
    CREATE TABLE t (
      id bigint AUTO_RANDOM NOT NULL,
      PRIMARY KEY (id)
    );
  up: |
    ALTER TABLE t CHANGE COLUMN id id bigint NOT NULL AUTO_RANDOM;
  down: |
    ALTER TABLE t CHANGE COLUMN id id bigint NOT NULL;

AddAutoRandomWithShardBits:
  legacy_ignore_quotes: false
  offline: true
  current: |
    CREATE TABLE t (
      id bigint NOT NULL,
      PRIMARY KEY (id)
    );
  desired: |
    CREATE TABLE t (
      id bigint AUTO_RANDOM(5) NOT NULL,
      PRIMARY KEY (id)
    );
  up: |
    ALTER TABLE t CHANGE COLUMN id id bigint NOT NULL AUTO_RANDOM(5);
  down: |
    ALTER TABLE t CHANGE COLUMN id id bigint NOT NULL;

RemoveAutoRandom:
  legacy_ignore_quotes: false
  offline: true
  enable_drop: true
  current: |
    CREATE TABLE t (
      id bigint AUTO_RANDOM(5) NOT NULL,
      PRIMARY KEY (id)
    );
  desired: |
    CREATE TABLE t (
      id bigint NOT NULL,
      PRIMARY KEY (id)
    );
  up: |
    ALTER TABLE t CHANGE COLUMN id id bigint NOT NULL;
  down: |
    ALTER TABLE t CHANGE COLUMN id id bigint NOT NULL AUTO_RANDOM(5);

CreateTableAutoRandomWithAutoIdCache:
  flavor: tidb
  legacy_ignore_quotes: false
  desired: |
    CREATE TABLE t (
      id bigint AUTO_RANDOM,
      PRIMARY KEY (id)
    );

CreateTableAutoIdCache:
  flavor: tidb
  legacy_ignore_quotes: false
  desired: |
    CREATE TABLE t (
      id bigint AUTO_INCREMENT,
      PRIMARY KEY (id)
    ) AUTO_ID_CACHE=1;

ChangeAutoIdCache:
  legacy_ignore_quotes: false
  offline: true
  current: |
    CREATE TABLE t (
      id bigint AUTO_INCREMENT,
      PRIMARY KEY (id)
    ) AUTO_ID_CACHE=100;
  desired: |
    CREATE TABLE t (
      id bigint AUTO_INCREMENT,
      PRIMARY KEY (id)
    ) AUTO_ID_CACHE=1;
  up: |
    ALTER TABLE t AUTO_ID_CACHE = 1;
  down: |
    ALTER TABLE t AUTO_ID_CACHE = 100;

CreateTableShardRowIdBits:
  flavor: tidb
  legacy_ignore_quotes: false
  desired: |
    CREATE TABLE t (
      a int,
      b int
    ) SHARD_ROW_ID_BITS=4 PRE_SPLIT_REGIONS=3;

ChangeShardRowIdBits:
  legacy_ignore_quotes: false
  offline: true
  current: |
    CREATE TABLE t (
      a int
    ) SHARD_ROW_ID_BITS=4;
  desired: |
    CREATE TABLE t (
      a int
    ) SHARD_ROW_ID_BITS=2;
  up: |
    ALTER TABLE t SHARD_ROW_ID_BITS = 2;
  down: |
    ALTER TABLE t SHARD_ROW_ID_BITS = 4;

AddShardRowIdBits:
  legacy_ignore_quotes: false
  offline: true
  current: |
    CREATE TABLE t (
      a int
    );
  desired: |
    CREATE TABLE t (
      a int
    ) SHARD_ROW_ID_BITS=4 PRE_SPLIT_REGIONS=3;
  up: |
    ALTER TABLE t SHARD_ROW_ID_BITS = 4;
    ALTER TABLE t PRE_SPLIT_REGIONS = 3;
  down: |
    ALTER TABLE t SHARD_ROW_ID_BITS = 0;
    ALTER TABLE t PRE_SPLIT_REGIONS = 0;

CreateTableClusteredPrimaryKey:
  legacy_ignore_quotes: false
  desired: |
    CREATE TABLE t (
      id bigint NOT NULL,
      PRIMARY KEY (id) /*T![clustered_index] CLUSTERED */
    );

CreateTableNonclusteredPrimaryKey:
  legacy_ignore_quotes: false
  desired: |
    CREATE TABLE t (
      id bigint NOT NULL,
      PRIMARY KEY (id) /*T![clustered_index] NONCLUSTERED */
    );

ClusteredIgnoredInComparison:
  legacy_ignore_quotes: false
  offline: true
  current: |
    CREATE TABLE t (
      id bigint NOT NULL,
      PRIMARY KEY (id) CLUSTERED
    );
  desired: |
    CREATE TABLE t (
      id bigint NOT NULL,
      PRIMARY KEY (id)
    );
  up: ""
  down: ""
